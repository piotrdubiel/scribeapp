package io.scribe.classifier;

import org.junit.Test;

import static org.junit.Assert.assertNotNull;

public class WebsocketTest {
    @Test
    public void shouldSendWithWebsocket() throws InterruptedException {
//        WebsocketRecognitionHandler websocketRecognitionHandler = new WebsocketRecognitionHandler();
//        websocketRecognitionHandler.start();
//        Thread.sleep(2000);
//        assertNotNull(websocketRecognitionHandler.getSocket());
    }
}
